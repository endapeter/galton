# Galton Board Research - Plan & Technical Overview

*Last updated: 2026-08-22*

---

## 1. Research Goals

The project investigates the Galton board as a physical random-process generator, starting from
`problem.txt`:

1. **Baseline phenomenon** - beads dropped on an evenly pegged board produce a binomial
   distribution (the discrete analogue of the normal / CLT outcome).
2. **Distribution shaping** - can *other* distributions be generated by varying board parameters
   (peg size, peg spacing/lattice, bead format/size, coefficient of restitution, funnel width)?
3. **CLT violation** - can the board produce a distribution that does *not* obey the central
   limit theorem in an i.i.d. scenario (heavy tails / infinite variance)? The notes flag the
   **arcsine law** as a target to reproduce and understand ("what is going on in the gaps in the
   arcsine law distribution?").
4. **Vibration** - what happens to the output distribution when the board vibrates? Open sub-
   questions from `notes.txt`: mean free path / mean free time between peg hits; possible
   **resonance** if the vibration frequency matches the peg-bounce frequency.
5. **Board as a distribution transform** - treat the board mathematically as a map from an input
   distribution to an output distribution:
   - funnel (drop-range) width vs. output standard deviation,
   - variance of the *input* distribution (e.g. Gaussian) vs. variance of the *output*.
6. **Brownian-motion framing** - treat vertical descent as time and horizontal motion as a 1-D
   random walk driven by peg impacts; study RMS displacement vs. board length.
7. **Variance convergence vs. bin resolution** - if variance is finite it should converge as bin
   resolution increases / ball count grows; otherwise it diverges - a fingerprint of
   CLT-violating (infinite-variance) behavior.
8. **Rigorous normality testing** - Q-Q plots with R² (linearity), skewness, kurtosis per
   configuration, rather than eyeballing histograms.

Secondary questions recorded in the notes: ball–ball interactions, finite-size ball/peg effects,
sand-vs-bead scaling, and the relationship between funnel size and grid proportionality.

### Experiment queue (from `notes.txt`, priorities set 2026-08-21)

**Done - scripts and results:**

- [x] R² vs. peg radius, iterated over peg distance - `step6_r2_vs_radius.py`,
      results in `figures/step6_r2_vs_radius/` (section 2).
- [x] σ(output) vs. funnel width - `step7_sigma_vs_funnel.py`, results in
      `figures/step7_sigma_vs_funnel/` (section 2).
- [x] σ(output) vs. σ(input) with a Gaussian input - `step8_gaussian_input.py`,
      results in `figures/step8_gaussian_input/` (section 2).

**Next:**

- [ ] Brownian-motion modeling: represent the horizontal motion as 1-D Brownian motion,
      estimate the effective diffusion coefficient, compare against the diffusion
      prediction (section 7, step 9).

**Backlog:**

- [ ] Reproduce the arcsine-law curve from the reference paper; explain the gaps in the distribution.
- [ ] `changing_rad.gif` re-run with higher restitution *e* and a narrow funnel.
- [ ] Variance vs. bin-resolution / ball-count matrix (finite- vs infinite-variance signature).
- [ ] Vibration experiments (moving pegs) - requires per-frame spatial-grid rebuilds.
- [ ] Ball-ball interactions - breaks the one-thread-per-ball independence assumption.

---

## 2. Current Status

### What is working today

- A fully **event-driven, analytic** simulation: no fixed time-step, no integration error.
  Collision times are found by solving polynomial equations exactly (quadratic for the
  detection line, linear for walls, quartic for pegs under gravity).
- A **parameter-sweep -> animation-frame pipeline**: for each sweep value the engine emits a
  histogram, a Q-Q plot with normality metrics, a raw-data CSV, and a single-ball verification
  trajectory figure, all indexed by frame number.
- The **GPU engine** (`galton_cuda/` package): one CUDA thread per ball, whole frame = one
  kernel launch. Run via `python3 -m galton_cuda` or imported as a library
  (`from galton_cuda import run_sweep`).
- Docker tooling for Windows + WSL2 with GPU passthrough, including smoke-test knobs
  (`GALTON_BALLS`, `GALTON_INTERVALS`), documented in `CUDA_GUIDE.md`. The Docker Desktop
  DNS problem that previously blocked image builds is resolved (Docker Desktop network DNS
  set to `8.8.8.8,1.1.1.1`); the image builds and GPU runs work.

### Experiment results (steps 6-8 of the queue)

All runs: 2000 balls per frame, defaults elsewhere; raw per-frame CSVs / PNGs in the
`figures/` subdirectories named below.

| Step | Question | Result |
|---|---|---|
| 6 | R² of normality vs. peg radius, per peg distance (`figures/step6_r2_vs_radius/`) | R² ≥ 0.99 for 30 of the 33 valid (distance, radius) pairs across distances 2.0-4.0 and radii 0.1-0.7 - normality is robust. Exceptions: (2.0, 0.5) and (2.5, 0.6) yielded 0 successful balls (inter-peg gaps close, nothing gets through) and (3.0, 0.7) dropped to R² = 0.65. |
| 7 | σ(output) vs. funnel half-width *v* (`figures/step7_sigma_vs_funnel/`) | σ_out rises only from 11.19 (v = 0.05) to 30.09 (v = 47.0) while the uniform input σ grows from 0.03 to 27.14: the board's intrinsic mixing (≈ 11) dominates until the input spread overtakes it. |
| 8 | Gaussian input σ_in -> output σ_out (`figures/step8_gaussian_input/`) | x ~ N(0, 47/3) through the widest funnel: σ_in = 15.63 -> σ_out = 19.68, ratio 1.26, 2000/2000 balls successful. First data point for goal 5b. |

### What remains open (relative to the goals)

- The CLT-violation hunt (arcsine-law reproduction) - noted as a to-do, not started.
- Brownian-motion / diffusion-coefficient analysis (step 9, next in the queue) - the per-frame
  CSVs needed for it already exist.
- Vibration / resonance experiments - not started; architecture-level change (moving pegs
  invalidate the prebuilt spatial grid).
- Ball–ball interactions - not started; breaks per-ball thread independence (would need a
  different parallelization strategy, e.g. batched time-stepping).
- Input-σ -> output-σ relationship - one data point so far (step 8); a σ_in sweep would map
  the full transform.

---

## 3. Current Technical Setup

### Environment

- **Host**: Windows 11. Source lives at `C:\Users\Enda\Data\Code\galton`.
- **Container runtime**: Docker with the WSL2 backend.
- **GPU**: NVIDIA driver stays on the Windows host and is passed into WSL2/containers
  (`libcuda` from the host; verified with `nvidia-smi` inside WSL per the Dockerfile header).
- **CUDA toolkit**: provided by the image `nvidia/cuda:12.4.1-devel-ubuntu22.04` - the toolkit
  (`nvcc` / NVVM) is what Numba's CUDA target links against **at JIT time**; it is not bundled
  with the driver.
- **Python**: Ubuntu 22.04's Python 3.10, isolated in a venv at `/opt/venv`
  (avoids PEP 668 "externally managed environment" errors).
- **Python deps** (`requirements.txt`, all unpinned): `numpy`, `numba`, `matplotlib`, `pandas`,
  `scipy`. Matplotlib runs headless (`MPLBACKEND=Agg`).
- **I/O model**: the repo folder is bind-mounted into the container at `/work`, so figures and
  CSVs written by the simulation land directly on the Windows filesystem.

### Run commands (see also `CUDA_GUIDE.md`)

```bash
# Build
docker build -t galton-cuda .

# Default sweep: X_DROP_RANGE (funnel width), 2000 balls, 10 frames
docker run --rm --gpus all -v "C:/Users/Enda/Data/Code/galton:/work" -w /work \
    galton-cuda python3 -m galton_cuda

# Smoke test
docker run --rm --gpus all -v "C:/Users/Enda/Data/Code/galton:/work" -w /work \
    -e GALTON_BALLS=200 -e GALTON_INTERVALS=2 \
    galton-cuda python3 -m galton_cuda

# Experiment scripts (steps 6-8; each writes its own figures/ subdirectory)
docker run --rm --gpus all -v "C:/Users/Enda/Data/Code/galton:/work" -w /work \
    galton-cuda python3 step6_r2_vs_radius.py      # or step7_sigma_vs_funnel.py / step8_gaussian_input.py
```

The CLI also accepts `--sweep`, `--low/--high/--steps`, `--balls`, `--bins`, `--output`,
repeatable `--set NAME=VALUE` overrides, and `--list-params` (see `galton_cuda/__main__.py`).

### Repository layout

```
galton/
├── galton_cuda/               # GPU engine package (Numba CUDA) - primary
│   ├── kernels.py             # device kernel, quartic solver, batch launcher
│   ├── geometry.py            # peg-lattice generation + spatial hash
│   ├── sweep.py               # per-frame orchestration + sweep controller
│   ├── visualization.py       # histogram, verification walk, Q-Q plot
│   └── __main__.py            # command-line entry point
├── step6_r2_vs_radius.py      # experiment: R² vs. radius per peg distance
├── step7_sigma_vs_funnel.py   # experiment: σ(output) vs. funnel width
├── step8_gaussian_input.py    # experiment: Gaussian input, σ_in vs. σ_out
├── problem.txt                # research questions
├── notes.txt                  # running research log / to-do list
├── Dockerfile                 # CUDA 12.4.1 devel image + venv + deps
├── requirements.txt           # numpy, numba, matplotlib, pandas, scipy (unpinned)
├── CUDA_GUIDE.md              # how to build/run the container
├── notebooks/                 # prototyping history (sympy, KDTree, optimization lab)
├── archived/                  # superseded engines (incl. galton_ultra.py) + analysis tools
└── figures/                   # (generated) per-frame PNGs/CSVs + experiment summaries
```

---

## 4. How the GPU / CUDA Processing Is Handled

### Execution model

- **One GPU thread per ball.** The entire trajectory - the event loop, all three event
  solvers, the peg search, and the reflection math - runs *on the device* (`simulate_trajectory_cuda`,
  `galton_cuda/kernels.py:183`). A whole frame of `n_balls` trajectories is therefore a
  **single kernel launch**.
- Launch configuration: `threads_per_block = 64`, `blocks = ceil(n_balls / 64)`
  (`galton_cuda/kernels.py:363-364`). Threads beyond `n_balls` exit immediately.
- Work distribution is trivially parallel because balls do not interact - which is precisely
  why ball–ball interaction remains an open research item.

### The device-side quartic solver

`np.roots` does not exist inside CUDA kernels, so the time-to-peg-impact quartic is solved by
`_quartic_smallest_root` (`galton_cuda/kernels.py:77`) **in closed form**:

- Normalize to monic, depress the quartic (`t = u − a/4` -> `u⁴ + p·u² + q·u + r`).
- Solve the resolvent cubic `z³ + 2p·z² + (p² − 4r)·z − q² = 0` for its **largest real root**
  (always ≥ 0 for real coefficients): Cardano when the discriminant ≥ 0, otherwise the
  trigonometric `k = 0` branch for three-real-root cases; real cube roots handle negative
  arguments (`_cbrt`).
- With `α = √z`, factor into two real quadratics `(u² + αu + β)(u² − αu + γ)`; each
  contributes up to two candidate roots. (`z ≤ 0` occurs only through round-off when
  `q ≈ 0` - the quartic is then effectively biquadratic and solved directly.)
- Each candidate passes through `_try_root` -> `_newton_polish`
  (`galton_cuda/kernels.py:47-76`): a Newton step on the original polynomial recovers
  closed-form round-off (worst near double roots), then the root must lie in the open
  interval `(t_lower, t_upper)`. NaN candidates from degenerate coefficients fail every
  comparison and are dropped, so numerical garbage can never become a collision time.
  The smallest surviving root wins, or `-1.0` if none. The upper bound is the current best
  collision time, so the search is progressively self-pruning.
- All arithmetic is real float64 with **no data-dependent iteration** anywhere.

### Per-batch data flow (`simulate_batch_cuda`, `galton_cuda/kernels.py:343`)

1. Host allocates device arrays and uploads inputs:
   `cuda.to_device` for `circles_centers`, `grid_data`, `grid_counts`, `x_inits`; device-side
   allocation for outputs `final_x` (float64) and `status` (int32).
2. One kernel launch; `cuda.synchronize()`; wall-clock timing printed per batch.
3. `final_x` and `status` are copied back to host NumPy arrays. That is the *entire* GPU↔CPU
   interface - two arrays per frame.

### What stays on the CPU and why

- Geometry generation and spatial-grid construction (cheap, once per frame).
- All rendering: histogram, Q-Q plot (pandas + scipy), and the single-ball **verification
  walk**, which re-simulates one random ball of the batch in pure Python with `np.roots` - a
  high-fidelity cross-check of the physics, and a visual diagnostic of what a trajectory looks
  like.
- The verification walk is capped at 500 steps; the GPU kernel at 1000 steps.

### Deviations from the CPU engine (documented in code comments)

- The CPU version's per-thread "seen pegs" dedup array is dropped on the GPU: re-testing a
  duplicate peg recomputes the same root, so the minimum is unchanged - dedup was a pure
  optimization, not a correctness requirement.
- All device math is **float64**.

---

## 5. Known Infrastructure Limitations (unfixed)

The container architecture is sound (toolkit in image, driver from host via WSL2,
`--gpus all`, bind mount for outputs). Known inefficiencies, roughly by impact:

- **Image size**: the `devel` image ships the entire toolkit (~5-7 GB uncompressed), but the
  Numba CUDA JIT needs only NVVM (`libnvvm.so`) and `ptxas`. Switching to
  `nvidia/cuda:12.4.1-base-ubuntu22.04` + `cuda-nvcc-12-4` + `cuda-nvvm-12-4` would cut
  multiple GB with identical behavior.
- **Unpinned `requirements.txt`**: Numba's CUDA target has been split into the separate
  NVIDIA-maintained `numba-cuda` package; a rebuild can silently produce an image where
  `from numba import cuda` fails. Pin exact versions (commit a `pip freeze`), adding
  `numba-cuda` if the resolved numba requires it.
- **Ephemeral containers re-pay JIT and font-cache costs on every run** (`--rm`): the Numba
  CUDA kernel is recompiled on every container start, and the matplotlib font cache is rebuilt
  at first `savefig`. Persist `NUMBA_CUDA_CACHE_DIR` and `~/.cache/matplotlib` on mounted
  volumes (or bake a warmed cache into the image).
- **`.dockerignore` misses `.git/`** (and any generated GIFs) - dead build-context upload on
  every build.
- **Runtime, not Docker, dominates wall-clock**:
  - `simulate_batch_cuda` re-uploads the immutable peg geometry every frame; device arrays
    could be created once per sweep and reused (the grid is ~200 KB, so this is a cleanliness
    fix more than a speedup at 2000 balls).
  - The per-frame pure-Python verification walk is likely the slowest stage; it should be
    env-gated (`GALTON_VERIFY=0`) for production sweeps.
  - float64 everywhere: consumer GeForce GPUs run FP64 at 1/32-1/64 of FP32 throughput; if
    float32 proves sufficient (needs a validation run), the kernel would speed up dramatically.
  - Frames are fully sequential; GPU computation of frame *N+1* could overlap CPU rendering of
    frame *N* (double buffering).
  - PNG writes cross the WSL2->Windows filesystem boundary at 300 dpi, three images per frame.

---

## 6. The Technical Pipeline, Precisely

End-to-end control flow per sweep frame. Default parameters live in
`galton_cuda/sweep.py` (`DEFAULT_PARAMS`); `GALTON_BALLS` / `GALTON_INTERVALS` environment
variables trim the run for smoke tests.

### Stage 0 - Sweep configuration (`galton_cuda/__main__.py` / `run_sweep`)

```
params = {
  N_FIRST_ROW: 51, N_ROWS: 50, DISTANCE: 2.0, H_INIT: 3.0, H_FINAL: 1.0,
  RADIUS: 0.7, X_DROP_RANGE: (-0.05, 0.05), WALL_PADDING: 0.5,
  COEFF_E: 0.1, CELL_SIZE: 2.0
}
SWEEP_PARAM = 'X_DROP_RANGE'; sweep_values = linspace(0.05, 47.0, 10)
```

For `X_DROP_RANGE`, each scalar sweep value `v` becomes the symmetric drop window `(-v, +v)`
(the "funnel width" experiment). CUDA availability is asserted up front with a clear error
message; `cuda.detect()` prints the device name. Any parameter can be swept or held constant
(`--set`), and the nested-sweep experiment scripts drive `run_sweep()` directly from Python.

### Stage 1 - Geometry generation (`generate_circle_geometry`, `galton_cuda/geometry.py`)

Staggered (hexagonal) peg lattice:

- Row 0 has `N_FIRST_ROW = 51` pegs; odd rows have 50 (the stagger).
- Horizontal pitch `d = 2.0`; vertical pitch `d·√3/2 ≈ 1.732` (equilateral-triangle packing).
- Row 0 sits at `y = -H_INIT = -3.0`; each next row steps down by the vertical pitch.
- Every peg has radius `0.7` - which is also the **collision distance**: the quartic below
  fires when the ball *center* is 0.7 from a peg *center*, so the ball itself is currently a
  **point mass** (finite ball size is an open research item).
- Side walls are placed at `WALL_DISTANCE = max|x_center| + r + WALL_PADDING = 50 + 0.7 + 0.5 = 51.2`.
- Total pegs: 25 rows × 51 + 25 rows × 50 = **2 525**.

### Stage 2 - Spatial hashing (`build_spatial_grid`, `galton_cuda/geometry.py`)

- Uniform grid with `cell_size = 2.0`; each peg is registered into **every cell its axis-aligned
  bounding box (`center ± radius`) overlaps**.
- Storage is dense and typed for the device: `grid_data[x_cell, y_cell, k]` = peg index or -1
  (int32, hard cap of 20 pegs per cell), `grid_counts[x_cell, y_cell]` = occupancy.
- Cell coordinates are offset by `(min_cx, min_cy)` so indices start at 0; lookups re-derive
  absolute cell indices and bounds-check against the grid extent.

### Stage 3 - Batch sampling

`n_balls = 2000` initial x positions drawn `~ Uniform(-v, +v)`; every ball starts at `y = 0`,
`vx = vy = 0` (dropped from rest above the peg field). `run_sweep(x_sampler=...)` can replace
the uniform sampler with any callable `(n_balls, drop_min, drop_max) -> ndarray` - the step-8
experiment uses this to feed `x ~ N(0, σ_in)` through the widest funnel.

### Stage 4 - GPU kernel: event-driven trajectory integration

Each thread runs the following loop (max 1 000 iterations) for its ball, with
`g = 9.81` and detection line at `y_target = -H_INIT - N_ROWS·DISTANCE·cos30° - H_FINAL ≈ -90.60`:

1. **Time-to-detection-line** - solve `y_imp + vy·t − ½g·t² = y_target`, a quadratic in t;
   take the smallest root `> 1e-5`.
2. **Time-to-wall** - linear in t: left wall if `vx < 0`, right wall if `vx > 0`; root must
   exceed `1e-5`.
3. **Peg search** - compute the ball's motion bounding box over `[0, min(t_line, t_wall)]`
   (including the parabola's vertex if the apex falls inside the interval), expand it by the
   collision radius, and enumerate all grid cells the box overlaps. For every candidate peg,
   solve the **quartic** in t obtained from `|p(t) − c|² = radius²` with
   `p(t) = p₀ + v·t − ½g·t²ŷ` (coefficients `c4 = g²/4, c3 = −vy·g, c2 = vx²+vy²−dy·g,
   c1 = 2(dx·vx+dy·vy), c0 = dx²+dy²−r²`), keeping the smallest real root in
   `(1e-5, current best)` via the closed-form device solver.
4. **Earliest event wins** among {line, peg, wall}:
   - **Detection line (event 0):** record `final_x = x_next`, set `status = 1`, thread returns.
   - **Peg (event 1):** specular reflection with restitution `e = 0.1`:
     `v' = v − (1+e)(v·n̂)n̂` where `n̂` is the contact normal - the normal velocity component
     reverses scaled by `e`, the tangential component is preserved. The ball is then nudged
     `1e-4` outward along the normal so the next iteration cannot re-detect the same contact.
   - **Wall (event 2):** `vx -> −e·vx`, `vy` unchanged, plus the same `1e-4` nudge.
5. If no event has a finite time, or the 1 000-step budget is exhausted, the ball is marked
   `status = 0` and `final_x = NaN` ("failed_or_out_of_bounds").

Note there is **no time-stepping anywhere**: between events the motion is the exact ballistic
parabola, so the only numerical artifacts are the root-finding tolerance (`1e-6` imaginary
part for "real", `1e-5` minimum time) and the post-collision nudges.

### Stage 5 - Results and analysis products (CPU)

For each frame index `XXX` (in the sweep's output directory):

| Artifact | File | Content |
|---|---|---|
| Raw data | `galton_ball_positions_XXX.csv` | `ball_id, initial_x, final_x, status` for all balls |
| Histogram | `galton_distribution_XXX.png` | 200 bins over `[-51.2, 51.2]`, success count annotated, sweep parameter in the title |
| Verification walk | `random_verification_path_XXX.png` | One random ball from the batch re-simulated in pure Python (`np.roots` quartics), trajectory drawn over the peg field, walls, and detection line |
| Q-Q plot | `galton_qq_plot_XXX.png` | `scipy.stats.probplot` vs. standard normal on the successful `final_x` values; annotates N, R², skewness, kurtosis (requires ≥ 30 valid points) |

Frames are produced sequentially for each sweep value; the file naming (zero-padded frame
index) is designed for later assembly into animations - the assembly step itself (ffmpeg /
ImageMagick) happens outside this repo.

### Physics summary

| Quantity | Value / rule |
|---|---|
| Gravity | 9.81 (units arbitrary, consistent) |
| Peg lattice | hexagonal, pitch 2.0, 51/50 alternating, 50 rows, 2 525 pegs |
| Collision distance (peg) | 0.7 from peg center (point-mass ball) |
| Restitution (pegs & walls) | e = 0.1 |
| Drop condition | from rest, y = 0, x ~ U(-v, v) or N(0, σ_in) via `x_sampler` |
| Detection line | y ≈ −90.60 (1 unit below the last row) |
| Numerical guards | t > 1e-5 per event; 1e-4 positional nudge after contacts |
| Step budgets | 1 000 (GPU kernel), 500 (CPU verification walk) |

---

## 7. Next Steps

**Infrastructure (small, quality-of-life):**

1. Slim the image: `base` + `cuda-nvcc-12-4` + `cuda-nvvm-12-4` (section 5, image size).
2. Pin `requirements.txt` (and add `numba-cuda` if needed) - commit a `pip freeze` (section 5).
3. Persist Numba/matplotlib caches via mounted volumes; add `.git` to `.dockerignore` (section 5).
4. Env-gate the verification walk so production sweeps skip the slow pure-Python stage.

**Science - immediate experiment queue:**

6. ~~R² vs. peg radius per peg distance~~ - **done**: `step6_r2_vs_radius.py`, results in
   `figures/step6_r2_vs_radius/` (section 2).
7. ~~σ(output) vs. funnel width~~ - **done**: `step7_sigma_vs_funnel.py`, results in
   `figures/step7_sigma_vs_funnel/` (section 2).
8. ~~Gaussian input through the widest funnel~~ - **done**: `step8_gaussian_input.py`, results
   in `figures/step8_gaussian_input/` (section 2). Natural follow-up: sweep σ_in to map the
   full input-σ -> output-σ transform (goal 5b).
9. **1-D Brownian-motion modeling - begin the mathematical representation.** Frame the
   motion analytically: vertical descent plays the role of time, horizontal displacement is
   a 1-D random walk driven by peg impacts. First steps:
   - Estimate the effective diffusion coefficient D from existing per-ball CSVs via
     `E[(Δx)²] = 2·D·t`, with t proportional to board length / number of peg rows crossed.
   - Compare the observed output distribution against the diffusion prediction
     `N(0, √(2·D·t))`.
   - Record where the i.i.d.-steps assumption behind the diffusion limit breaks (e.g. peg
     radius comparable to the gap, or a funnel so wide that the initial x dominates).
   - Literature pass: Galton-board-as-Brownian-motion references (goal 6).

**Science - longer term:**

10. Reproduce the arcsine-law curve from the reference paper and explain the gaps (goal 3).
11. Variance-convergence vs. bin-resolution matrix - the finite/infinite-variance fingerprint
    (goal 7); the archived `galton_variance.py` is the starting point, now GPU-accelerated.
12. Re-run `changing_rad.gif` at higher restitution *e* with a narrow funnel (reuses the
    radius sweep from step 6).
13. Vibration experiments - requires rethinking the static spatial grid (rebuild per frame or
    analytic moving-peg solvers) and adds resonance/mean-free-time diagnostics (goal 4).
14. Ball-ball interactions - the largest architectural change; breaks thread independence,
    likely needs batched time-stepping or a different decomposition.

---

## Appendix: Source-map quick reference

| Concern | Location |
|---|---|
| Research questions | `problem.txt` |
| Research log / to-dos | `notes.txt` |
| GPU kernel (one thread per ball) | `galton_cuda/kernels.py` `simulate_trajectory_cuda` |
| Device quartic solver (closed-form Ferrari/Cardano) | `galton_cuda/kernels.py` `_quartic_smallest_root` |
| Batch launcher / device transfers | `galton_cuda/kernels.py` `simulate_batch_cuda` |
| Geometry generation | `galton_cuda/geometry.py` `generate_circle_geometry` |
| Spatial hash build | `galton_cuda/geometry.py` `build_spatial_grid` |
| Verification walk (CPU) | `galton_cuda/visualization.py` `simulate_and_visualize_trajectory_optimized` |
| Q-Q plot generation | `galton_cuda/visualization.py` `generate_qq_plot` |
| Per-frame orchestration | `galton_cuda/sweep.py` `run_frame` |
| Sweep controller | `galton_cuda/sweep.py` `run_sweep` (optional `x_sampler` input hook) |
| Command-line interface | `galton_cuda/__main__.py` |
| Experiment: R² vs. radius per distance | `step6_r2_vs_radius.py` |
| Experiment: σ(output) vs. funnel width | `step7_sigma_vs_funnel.py` |
| Experiment: Gaussian input σ_in -> σ_out | `step8_gaussian_input.py` |
| CPU predecessor engine | `archived/galton_ultra.py` |
| Docker build/run docs | `Dockerfile` (header comments), `CUDA_GUIDE.md` |
